# Round 9 Task — Add Request Logging Middleware

Add simple request logging middleware to `back/main.py`.

- Log each request: method, path, status code, and response time in milliseconds.
- Use Python's built-in `logging` module, log at INFO level.
- Format: `GET /api/services 200 12ms`

Small change — just the middleware. Update `back/LLD.md` briefly. Reply in `back/REPLY.md`.
