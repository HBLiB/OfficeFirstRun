# Round 11 Reply

## What I did
- Created `front/src/components/ScrollToTop.jsx` — uses `useLocation` + `useEffect` to scroll to top on every route change, renders nothing.
- Updated `front/src/App.jsx` — imported and placed `<ScrollToTop />` inside the router wrapper, above the navbar.

## Files created
- `src/components/ScrollToTop.jsx`

## Files modified
- `src/App.jsx` — import + render ScrollToTop
