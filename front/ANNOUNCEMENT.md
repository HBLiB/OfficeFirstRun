# Round 8 Task — Add Footer Component

Create a reusable `Footer` component (`front/src/components/Footer.jsx`) and include it in `App.jsx`.

Contents:
- "© 2026 H-Network" on the left
- Links to Home, Services, Lab, Contact on the right
- Dark-themed, subtle border-top, consistent with existing dark style in `index.css`

Keep it simple — just the component, the CSS, and the App.jsx import. Reply in `front/REPLY.md`.
