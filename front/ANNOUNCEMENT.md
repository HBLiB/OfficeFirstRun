# Round 9 Task — Add Loading Spinners

Add a simple loading state to the pages that fetch from APIs: Services, Lab, and the Case Detail page.

- While data is loading, show a centered "Loading..." text or a simple CSS spinner.
- Add a `.loading-spinner` CSS class in `index.css` — a simple CSS-only spinning circle is fine.
- If fetch fails (and no mock fallback), show a brief error message.

Small change — just add loading/error states to the existing fetch logic. Reply in `front/REPLY.md`.
